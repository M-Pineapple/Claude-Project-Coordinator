import XCTest
@testable import ProjectCoordinator

final class ProjectCoordinatorTests: XCTestCase {
    func testExample() throws {
        // This is a basic test to ensure the test target compiles
        XCTAssertTrue(true)
    }
}
