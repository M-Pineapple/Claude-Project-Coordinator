import Foundation

/// Single source of truth for the published CPC version.
public enum CPCVersion {
    public static let string = "1.4.0"
    public static let serverName = "project-coordinator"
}
